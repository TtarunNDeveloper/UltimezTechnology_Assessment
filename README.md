#Install express to run the file
- npm install express

#Add localhost url to the port running
- Example port is 300, then the URL is http://localhost:3000
- then API will look like http://locahost:3000/books and etc.
